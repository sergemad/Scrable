#include "Partie.hpp"

#include <iostream>
#include <cassert>

int main (){
    /*printf("Debut ");

    saclettre bag;
    printf("initialisation terminer \n");

    bag.affichage();
    pion p=bag.pioche();
    printf("%c de valeur %i \n",p.lettre,p.valeur);
    //bag.affichage();*/
    /*
    Arbre_G Ar;
    char mot[] ={'A','B','A','C','A','\n'};
    Ar.recherche(mot);
    char mots[]={'Z','O','E','E','\n'};
       Ar.recherche(mots);
    char motss[] ={'A','B','A','C','A','Z','\n'};
    Ar.recherche(motss);
    */
    /*
    char mots[]={'Z','O','E','E','\n'};
    Ar.ensemble(mots);
    char mot[]={'B','O','U','D','E','\n'};
    Ar.ensemble(mot);
    //char motss[]={'B','E','E'};
    //Ar.ajout_mot(motss);


    //A.recherche(mot);
    //printf("le mot %s est %d \n",mot,A.recherche(mot));*/
   /* noeud A,B;
    printf("le noeud A a t il un voisin ? %d \n",A.vef_voisin('K'));
    A.suivant[int('K')-65]=&B;
    A.suivant[int('L')-65]=&B;
    printf("le noeud A a t il un voisin ? %d \n",A.vef_voisin('K'));
    printf("le noeud A a t il un voisin ? %d \n",A.vef_voisin('L'));

   printf("le noeud A a t il un voisin ? %d \n",A.vef_voisin('A'));*/
    
    Partie Game;
    
    return 0;
}
